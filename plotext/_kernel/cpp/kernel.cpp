// Kernel entry point: pulls in every kernel source via 00_header.h; main() exists only so the file can be compiled as an executable for testing

using namespace std;

#include "00_header.h"


int main() {
    return 0;
}
