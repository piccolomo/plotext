// Standalone entry point, empty: the kernel is used as a library, and this main() only exists so it can also be compiled as a program, for quick experiments.

using namespace std;

#include "00_header.h"

int main() {
    return 0;
}
